

//在handler当中使用sql、meddler针对handler当中的数据请求进行处理。 
//meddler需要使用的到modler当中的结构体。
//sql需要使用到中间件加载进来的db。在handler当中加载了这个db，然后需要使用者个db去执行sql语句，这些个sql语句，就可以专门保留定义成为一个字符串。统一存放。 
//当我们需要执行具体某个语句时，再去找到这个语句执行即可。 

//语句处理本质上还是通过使用database/sql去执行sql语句。只是可以另外借助meddler工具，来帮助我们做结构对象和数据的转化。
